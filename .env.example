# Authentication Mode
# Options: none, forwardauth
AUTH_MODE=none

# Data Directory
DATA_DIR=/data

# Database Path
DB_PATH=/data/db/app.sqlite

# Photos Directory
PHOTOS_DIR=/data/photos

# Frontend Configuration
# API Base URL - leave empty to use relative URLs (recommended for self-hosted)
# This allows the frontend to work from any device on your network
# Only set this if you need to point to a different backend server
VITE_API_BASE_URL=

# Number of bags for doll storage
VITE_BAGS_COUNT=3
